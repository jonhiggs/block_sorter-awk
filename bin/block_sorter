#!/usr/bin/env bash
source $(dirname $0)/../lib/shell_functions.inc

INPUT_FILE=$1
ITERATIONS=${2:-$(block_count)}

for i in $(seq 0 ${ITERATIONS}); do
  block_data=$(block $i)
  echo "${block_data}" | sed '1!d'
  echo "${block_data}" | sed '1d' | sort
done
