#!/usr/bin/env bash
ROOT_DIR="$(dirname $0)/../"

block() {
  local input=$1
  awk \
    -f ${ROOT_DIR}/lib/block_split.awk \
    block=$2 \
    "${input}"
}

block_data=$(block $1 $2)

echo "${block_data}" | sed '1!d'
echo "${block_data}" | sed '1d' | sort
